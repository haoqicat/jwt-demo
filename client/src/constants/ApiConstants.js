const SERVER = `http://localhost:3009`

export const LOGIN_URL = `${SERVER}/user/login`
export const SIGNUP_URL = `${SERVER}/user/signup`
export const POST_URL = `${SERVER}/post`
export const GET_POSTS_URL = `${SERVER}/posts`
